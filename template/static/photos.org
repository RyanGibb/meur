---
title: Photos
feed: photos
---

$if(html)$
#+begin_export html
<div class="gallery">
    <div>
        $for(photos)$
        <div id="$title$">
            <a href="$url$">
                <img src="$thumb$">
            </a>
            <div style="display: flex; gap: 4px; align-items: center;">
                <a href="#$title$" class="anchor" aria-hidden="true">§</a>
                $if(published)$
                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32" width="18" height="18" fill="none" stroke="currentcolor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2">
                    <path d="M2 6 L2 30 30 30 30 6 Z M2 15 L30 15 M7 3 L7 9 M13 3 L13 9 M19 3 L19 9 M25 3 L25 9" />
                </svg>
                <span>$published$</span>
                $endif$
            </div>
            $if(lat)$
            $if(lon)$
            <div style="display: flex; gap: 4px; align-items: center;">
                <a href="https://www.openstreetmap.org/?mlat=$lat$&mlon=$lon$#map=17/$lat$/$lon$" style="align-items: center;">
                    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32" width="18" height="18" fill="none" stroke="currentcolor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2">
                        <circle cx="16" cy="11" r="4" />
                        <path d="M24 15 C21 22 16 30 16 30 16 30 11 22 8 15 5 8 10 2 16 2 22 2 27 8 24 15 Z" />
                    </svg>
                    $if(loc)$
                    $loc$
                    $else$
                    Location
                    $endif$
                </a>
            </div>
            $endif$
            $endif$
            <div style="display: flex; gap: 4px; align-items: center;">
                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32" width="18" height="18" fill="none" stroke="currentcolor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2">
                    <path d="M2 8 L 9 8 12 4 20 4 23 8 30 8 30 26 2 26 Z" />
                    <circle cx="16" cy="16" r="5" />
                </svg>
                $if(make)$
                <span>$make$</span>
                $endif$
                $if(model)$
                <span>$model$</span>
                $endif$
                $if(focallength)$
                <span>$focallength$mm</span>
                $endif$
                $if(aperture)$
                <span>f/$aperture$</span>
                $endif$
                $if(exposure)$
                <span>$exposure$</span>
                $endif$
                $if(iso)$
                <span>ISO $iso$</span>
                $endif$
            </div>
        </div>
        $endfor$
    </div>
</div>
#+end_export
$else$
$for(photos)$
#+begin_export markdown
### $title$

![$title$]($url$)
![thumb/$title$]($thumb$)

$if(published)$Date: $published$$endif$
$if(lat)$$if(lon)$[$if(loc)$$loc$$else$Location$endif$](https://www.openstreetmap.org/?mlat=$lat$&mlon=$lon$#map=17/$lat$/$lon$)$endif$$endif$
$if(make)$$make$$endif$ $if(model)$$model$$endif$ $if(focallength)$$focallength$mm$endif$ $if(aperture)$f/$aperture$$endif$ $if(exposure)$$exposure$$endif$ $if(iso)$ISO $iso$$endif$
#+end_export
$endfor$
$endif$

Reverse geocoding provided by OpenStreetMap's [[https://nominatim.openstreetmap.org/][Nominatim]].
